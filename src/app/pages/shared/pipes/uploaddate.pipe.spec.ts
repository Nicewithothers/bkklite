import { UploaddatePipe } from './uploaddate.pipe';

describe('UploaddatePipe', () => {
  it('create an instance', () => {
    const pipe = new UploaddatePipe();
    expect(pipe).toBeTruthy();
  });
});
